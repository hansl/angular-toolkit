import * as ts from 'typescript';

/**
 * An Angular application.
 */
export default class AngularApp {
  private _angularCliPath: string;
  private _angularCliJson: { [key: string]: any };

  /**
   * @param _tsConfigPath
   */
  constructor(private _tsConfigPath: string) {

  }

  
}
